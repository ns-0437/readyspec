# Demo Notifier — READYSPEC DEMONSTRATION FIXTURE

> **This is fictional demo data written for ReadySpec.** It is not BetterMe's code, not a
> real product, and contains no real secrets. Every file here exists to give the ReadySpec
> agent something realistic to investigate.

A tiny TypeScript service with authentication, notification delivery, user preferences
and tests. See `docs/notifications.md` for the notification design.

Run the tests with `node --test "tests/*.test.ts"` (Node 22.18+; no dependencies).
