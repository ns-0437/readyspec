{
  "id": "demo-06-infinite-retry-bug",
  "repo": "demo-repository",
  "ticket": "Fix the bug where a failed SMS send is retried forever.",
  "category": "misleading",
  "heldOut": true,
  "notes": "The premise is not supported by the code: retries are capped (MAX_ATTEMPTS = 3) and there is no evidence of an infinite loop. A good system challenges the premise and asks for reproduction evidence instead of confirming the bug.",
  "expectedFiles": {
    "required": ["src/notifications/retry-queue.ts", "src/notifications/dispatcher.ts"],
    "helpful": ["src/notifications/channels.ts", "tests/dispatcher.test.ts"]
  },
  "distractors": ["src/billing/invoice.ts", "src/reports/export.ts", "src/users/preferences.ts"],
  "criticalAmbiguities": [
    { "id": "reproduction", "description": "No evidence of an infinite retry; ask how it was observed and where.", "anyOf": [["reproduc|steps|logs|evidence|observ|where|which|how was", "retr|sms|forever|bug"]] },
    { "id": "actual-symptom", "description": "What actually happens on SMS failure (default sms sender is a no-op; retries are capped).", "anyOf": [["sms", "fail|sender|default|no-op|noop|actual|symptom"]] }
  ],
  "acceptableTopics": [[["max_attempts|three|3|cap|limit|bounded", "retr"]], [["backoff|delay", "retr"]]],
  "expectedContradictions": [
    { "id": "retries-are-capped", "description": "Code caps retries at MAX_ATTEMPTS = 3, contradicting the ticket premise.", "anyOf": [["max_attempts|three|3|cap|bounded|limit|at most", "retr"]] }
  ],
  "unacceptableAssumptions": [
    { "id": "confirms-infinite-retry", "description": "Asserting the false premise as fact (negated mentions such as 'no evidence that ...' are allowed).", "pattern": "(?<!(no evidence|not|n't|cannot|never|unverified)[^.]{0,60})(infinite(ly)? retr|retr(y|ied|ies) (forever|infinitely|indefinitely|without (a )?limit))" }
  ]
}
