# Shop Orders — READYSPEC EVALUATION FIXTURE

> Fictional demo code written for ReadySpec's benchmark. Not a real product, not BetterMe's code.

A tiny order service: checkout, coupons, tax, refunds, stock and order emails.
See `docs/orders.md`. Run tests with `node --test "tests/*.test.ts"` (Node 22.18+).
